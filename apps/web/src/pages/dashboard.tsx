import { AppShell } from "../components/layout/app-shell";

export default function DashboardPage() {
  return (
    <AppShell>
      <div className="p-6">
        <p className="text-muted-foreground">
          Dashboard — expense table goes here (Task 5+)
        </p>
      </div>
    </AppShell>
  );
}
